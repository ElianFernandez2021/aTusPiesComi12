<!DOCTYPE html>
<html lang="es">
<%- include('../partials/adminHead')  %> 
<body>
    <%- include ('../partials/adminHeader') %> 
    <h2>Categoria: <%= categories[category_id-1].name %> </h2>
    <div class="categories-product">
        <% products.forEach(product => { %>
            <% if (product.category.name === categories[category_id-1].name) { %>
                <div class="products">
                   <p>Nombre: <%= product.name%> </p> 
                   <% product.images.forEach(imagenes => { %>
                       <img class="product-image" src="/images/products/<%- imagenes.image %> " alt="ID:<%= product.id%> ">
                   <% }) %>
                   <p>Descripcion: <%= product.description%> </p>
                   <p>Talle: 
                       <%- product.size %> 
                   </p> 
                   <p>Color: 
                       <% product.colors.forEach(color => { %>
                           <%= color.name %> 
                       <% }) %>
                   </p>
                   <p>Precio: <%- product.price%> </p>
                   <p>Marca:  
                       <%= product.marca.mark %>
                   </p>
                   <form action="/admin/products/edit/<%- product.id%> " method="get">
                       <button type="submit">Editar <i class="fas fa-edit"></i></button>
                   </form>
                   <form action="/admin/products/category/<%- product.id%>?_method=delete" method="post">
                       <button type="submit">Eliminar <i class="far fa-trash-alt"></i></button>
                   </form>
               </div>             
            <% } %>
        <% }) %>
    </div>
</body>
</html>